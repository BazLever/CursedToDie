using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerDeadBody : MonoBehaviour
{

    //Rigidbody rb;

    void Start()
    {
        //rb = GetComponent<Rigidbody>();
       //rb.AddForce((Vector3.up * 100 + Vector3.right * Random.Range(10, 100)));
    }


    void Update()
    {
        
    }
}
